<?xml version="1.0" encoding="utf-8"?>

<!-- Main screen layout -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!-- Top fragment (Spinner) -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/topFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:name="com.example.cis436_project3.ui.selector.BreedSelectorFragment" />

    <!-- Bottom fragment (details + image) -->

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/bottomFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:name="com.example.cis436_project3.ui.details.BreedDetailsFragment" />

</LinearLayout>